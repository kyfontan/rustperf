[package]
name = "machine_oriented_lints"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[package.metadata.rust-analyzer]
rustc_private = true
